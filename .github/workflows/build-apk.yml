# 自动打包 APK
# 推送到 GitHub 后自动编译 APK，可在 Actions 页面下载
name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # 手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Buildozer & dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            git zip unzip openjdk-17-jdk \
            python3-pip autoconf libtool \
            build-essential ccache libssl-dev \
            libncurses5-dev libncursesw5-dev \
            zlib1g-dev liblzma-dev \
            libsqlite3-dev libffi-dev \
            libbz2-dev pkg-config
          pip install --upgrade buildozer cython

      - name: Cache Buildozer
        uses: actions/cache@v4
        with:
          path: ~/.buildozer
          key: ${{ runner.os }}-buildozer-${{ hashFiles('buildozer.spec') }}

      - name: Build APK
        run: |
          # 创建 .buildozer 目录并强制安装 SDK 和接受许可证
          mkdir -p ~/.buildozer/android/platform
          buildozer -v android debug 2>&1 || true
          # 如果第一次因为许可证失败，手动接受并重试
          yes | ~/.buildozer/android/platform/android-sdk/cmdline-tools/latest/bin/sdkmanager --licenses 2>/dev/null || true
          # 第二次构建
          buildozer -v android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: 消个箭头-APK
          path: bin/*.apk
